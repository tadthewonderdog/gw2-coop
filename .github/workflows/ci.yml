name: CI

on:
  push:
    branches: [main, develop, github-integration]
  pull_request:
    branches: [main, develop]
  workflow_dispatch: # Allow manual triggering

# Security: Restrict permissions
permissions:
  contents: read
  checks: write
  pull-requests: write
  security-events: write

env:
  NODE_VERSION: '20'
  WORKING_DIRECTORY: .
  PROD_RELEASE: ${{ github.event_name == 'pull_request' && github.event.action == 'closed' }}
  PR_NUMBER: ${{ github.event.number }}
  PREVIEW_URL: "https://pr${{ github.event.number }}-gw2-coop-preview.tadthewonderdog.workers.dev"

jobs:
  create_preview_comment:
    name: Create preview comment for later updating
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    if: github.event_name == 'pull_request' && github.event.action == 'open'
    steps:
      - name: Create preview comment for later updating
        uses: thollander/actions-comment-pull-request@v3
        with:
          message: |
            🚀 *Reserved for preview URL
            📍 **URL:** COMINT SOON
          comment-tag: preview_url
          create-if-not-exists: true
          pr-number: ${{ github.event.number}}

  # dump_contexts_to_log:
  #   runs-on: ubuntu-latest
  #   # if: ${{ false }}
  #   steps:
  #     - name: Check if not prod
  #       if: ${{ env.PROD_RELEASE == 'false' }}
  #       run: echo blarg not prod
  #     - name: Check if prod
  #       if: ${{ env.PROD_RELEASE == 'true' }}
  #       run: echo blarg2 prod

  #     - name: PROD_RELEASE
  #       run: echo ${{ env.PROD_RELEASE }} ; echo ${{ github.event_name == 'pull_request' && github.event.action == 'closed' }}
  #     - name: Dump env context
  #       env:
  #         ENV_CONTEXT: ${{ toJson(env) }}
  #       run: echo "$ENV_CONTEXT"
  #     - name: Dump GitHub context
  #       env:
  #         GITHUB_CONTEXT: ${{ toJson(github) }}
  #       run: echo "$GITHUB_CONTEXT"
  #     - name: Dump job context
  #       env:
  #         JOB_CONTEXT: ${{ toJson(job) }}
  #       run: echo "$JOB_CONTEXT"
  #     - name: Dump steps context
  #       env:
  #         STEPS_CONTEXT: ${{ toJson(steps) }}
  #       run: echo "$STEPS_CONTEXT"
  #     - name: Dump runner context
  #       env:
  #         RUNNER_CONTEXT: ${{ toJson(runner) }}
  #       run: echo "$RUNNER_CONTEXT"
  #     - name: Dump strategy context
  #       env:
  #         STRATEGY_CONTEXT: ${{ toJson(strategy) }}
  #       run: echo "$STRATEGY_CONTEXT"
  #     - name: Dump matrix context
  #       env:
  #         MATRIX_CONTEXT: ${{ toJson(matrix) }}
  #       run: echo "$MATRIX_CONTEXT"


  # Security and dependency checks
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Full history for better analysis

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ./package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: .

      - name: Run security audit
        run: npm audit --audit-level=high
        working-directory: .

      - name: Check for known vulnerabilities
        run: npx better-npm-audit audit --level high
        working-directory: .

  # Code quality and linting
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ./package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: .
      - name: Lint code
        run: npm run lint
        working-directory: .
      - name: Check formatting
        run: npm run format:check
        working-directory: .
      - name: Validate package structure
        run: npm run validate
        working-directory: .

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ./package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: .
      - name: Type check
        run: npm run type-check
        working-directory: .

  test:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ./package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: .
      - name: Install Playwright browsers
        run: npx playwright install --with-deps
        working-directory: .
      - name: Run tests with coverage
        run: npm run test:coverage
        working-directory: .
      - name: Upload coverage reports
        uses: codecov/codecov-action@18283e04ce6e62d37312384ff67231eb8fd56d24 # v5.4.3
        with:
          directory: ./coverage
          fail_ci_if_error: false
          verbose: true
      - name: Check coverage threshold
        run: |
          COVERAGE=$(npm run test:coverage --silent | grep -o 'All files.*[0-9]\+\.[0-9]\+' | grep -o '[0-9]\+\.[0-9]\+' | head -1)
          echo "Coverage: $COVERAGE%"
          if (( $(echo "$COVERAGE < 30" | bc -l) )); then
            echo "❌ Coverage $COVERAGE% is below 30% threshold"
            exit 1
          else
            echo "✅ Coverage $COVERAGE% meets threshold"
          fi
        working-directory: .

  # Build and artifact creation
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [security, lint, type-check, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ./package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: .
      # - name: Cache GW2 achievement data
      #   run: npm run fetch:gw2-achievement-data
      #   working-directory: .
      - name: Build Preview application
        if: github.event.action != 'closed'
        run: npm run build:preview
        working-directory: .
      - name: Build application
        if: github.ref == 'refs/heads/main' && github.event.action == 'closed'
        run: npm run build
        working-directory: .
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ github.sha }}
          path: ./dist
          retention-days: 30
      - name: Analyze bundle size
        run: |
          echo "📦 Bundle Analysis:"
          du -sh dist/
          echo "📊 CSS Analysis:"
          npm run analyze:css 2>/dev/null || echo "CSS analysis not available"
          echo "✅ Build completed successfully"
        working-directory: .

  # Deploy preview for PRs using GitHub Pages
  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    needs: build
    # if: github.event_name == 'pull_request' && github.event.action != 'closed'
    # environment:
    #   name: preview
    #   url: https://pr${{ github.event.number }}.wonderdog.win
    permissions:
      contents: read
      pull-requests: write
    concurrency:
      group: "preview-${{ github.event.number }}"
      cancel-in-progress: true
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-${{ github.sha }}
          path: dist
      - name: Deploy to Cloudflare Workers
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          echo Installing wrangler
          npm i wrangler@4

          if [ "${{ env.PROD_RELEASE }}" = "true" ]
          then
            echo "NOT DEPLOYING TO PROD"
          else
            echo Deploying as pr${{ github.event.number }}
            echo npx wrangler versions upload --preview-alias pr${{ github.event.number }}
            echo "✅ Build completed successfully"
          fi
        working-directory: . # dist/gw2_coop
        
      # - name: Deploy to Cloudflare Workers (Preview)
      #   id: deploy
      #   uses: cloudflare/wrangler-action@v3
      #   with:
      #     wranglerVersion: 4
      #     apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     command: versions upload --preview-alias pr${{ github.event.number }}
      #     preCommands: find .
      #     postCommands: find .
      #     packageManager: /opt/hostedtoolcache/node/20.19.4/x64/bin/npm
      #     workingDirectory: dist/gw2_coop
        # working-directory: .
      - name: Comment PR
        uses: thollander/actions-comment-pull-request@v3
        if: ${{ env.PROD_RELEASE == 'false' }}
        with:
          message: |
            🚀 **Preview deployed successfully!**
            📍 **URL:** [On Cloudflare](https://pr${{ github.event.number }}-gw2-coop-preview.tadthewonderdog.workers.dev)
            
            This preview will be automatically updated when you push new commits to this PR.`
          comment-tag: preview_url
          pr-number: ${{ github.event.number}}
      - name: Delete a comment
        uses: thollander/actions-comment-pull-request@v3
        if: ${{ env.PROD_RELEASE == 'true' }}
        with:
          comment-tag: preview_url
          mode: delete
  

  # Production deployment using Cloudflare Workers
  # deploy-production:
  #   name: Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main' && github.event.action == 'closed'

  #   # Production deployment to Cloudflare Workers
  #   steps:
  #     - name: Download build artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: build-artifacts-${{ github.sha }}
  #         path: dist
  #     - name: Deploy to Cloudflare Workers (Production)
  #       uses: cloudflare/wrangler-action@v3
  #       with:
  #         apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  #         accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #         command: deploy
  #         packageManager: npm
  #         workingDirectory: .
